#include <iostream>
#include <cmath>

int main() {
    int a, b, c;

    for (a = 1; a <= 20; a++) {
        for (b = a; b<= 20; b++) {
            double c_double = sqrt (a*a + b*b);
        }
    }
}